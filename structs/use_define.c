
#include <stdio.h>
#define SIZE 50
#define max(a,b) a > b ? a : b    // Macro

void main()
{
  int a[SIZE];
  int i, sum, avg;

     big = max(x,y);     //  x > y ? x : y

     for(i = 0; i < SIZE; i ++)
     {
       // fill array
     }

     for(i = 0; i < SIZE; i ++)
     {
         sum += a[i];
     }

     avg = sum / SIZE;

}
